# Model Weights

This directory is intended to store the downloaded and fine-tuned model weights (e.g., Pythia-70m).
Model weights are generally too large to upload directly to GitHub without using Git LFS. 
Please download the models via the provided scripts or configure Git LFS if you intend to track them.
